<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="paymentcom.parking.jorge.parkingpayment" >

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".DashboardActivity"
            android:label="@string/app_name" >
        </activity>
        <activity
            android:name=".TimerCountActivity"
            android:label="@string/title_activity_timer_count" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".PinCheckerActivity"
            android:label="@string/title_activity_pin_checker" >
        </activity>
        <activity
            android:name=".PaymentsCongratsActivity"
            android:label="@string/title_activity_payments_congrats" >
        </activity>
    </application>

</manifest>
